import { nanoid } from "nanoid";

const dataInit = [
  { name: "apples", completed: false, id: nanoid() },
  { name: "milk", completed: false, id: nanoid() },
  { name: "bread", completed: false, id: nanoid() },
];

export default dataInit;
